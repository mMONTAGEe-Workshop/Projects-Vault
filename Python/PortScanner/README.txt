to run: python3 PortScanner.py <ip>

This is not very efficient or fast port scanner, at this point just use NMAP, use this as backup.

I do not know Python Syntax to build an normal problem check systems that i could do with C++, so be gentle with the program